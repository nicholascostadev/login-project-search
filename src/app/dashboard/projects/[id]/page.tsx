import { RichTextEditor } from "@/components/rich-text-editor";

export default function ProjectPage() {
  return (
    <main className="flex container min-h-screen flex-col items-center justify-start py-10">
      <RichTextEditor />
    </main>
  );
}
